version: '3.8'

services:
  local-ai:
    image: localai/localai:latest-gpu-nvidia # Or latest for CPU
    container_name: local-ai
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8333/readyz"]
      interval: 1m
      timeout: 10s
      retries: 3
    ports:
      - "8333:8333"
    environment:
      - MODELS_PATH=/models
      - DEBUG=true
    volumes:
      - ./Data/LocalAI/models:/models
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
